#include <stdio.h>
#include <stdlib.h>

int main(void) {

    // Comentários de uma linha iniciam com // (duas barras).
    // A função printf direriona o texto inserido (entre aspas duplas) para saída ádrão.
    printf("Ola, mundo!!");
    printf("Curso de C");

    return 0;
}